//Use this file when hacking on the compiler

module Main;


struct:T {
	let:Bool field;
	let:i32 other_field;
}

test "Reproduction" {
	let:T instance = new:T::{
		.field = false;
		.other_field = 42;
	};

	return instance.field;
}
