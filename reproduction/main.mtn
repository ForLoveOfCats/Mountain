//Use this file when hacking on the compiler

module Main;


// struct:T {
// let:Bool field;
// }

enum:Enum [first, second]

test "Reproduction" {
	// func:T function() {
	// return new:T::{
	// .field = true;
	// };
	// }

	// return function().field;

	func:Enum function() {
		return Enum.second;
	}
	let:Enum instance = function().second;
	// return instance != Enum.first;
	return true;
}
